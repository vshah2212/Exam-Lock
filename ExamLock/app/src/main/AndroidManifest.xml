<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vshah2212.examlock">

    <!-- To auto-complete the email text field in the login form with the user's emails -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/examlock"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/examlock"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".ParentRegLog" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".StudParConn" />
        <activity android:name=".StudentMain">

        </activity>
        <activity android:name=".ParentMain"></activity>
    </application>

</manifest>